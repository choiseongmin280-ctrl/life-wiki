# Open Questions — Archive

Resolved question rows (`answered` / `dropped`), newest first — the audit trail of every gap ever
raised. Rows arrive here from [[open-questions]] once closed, and are not edited afterward except
to receive a newly resolved row. Not read during normal operation; open the active list instead.

| Asked | Question | Page | Status |
| --- | --- | --- | --- |
