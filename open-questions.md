# Open Questions

Unresolved gaps from ingestions, newest first. When one is answered in any session: update the
affected pages citing `chat YYYY-MM-DD`, tick the checkbox on the page, then MOVE its row to the
top of [[open-questions-archive]] with status `answered` or `dropped`. This file holds only rows
still `open`, so it stays short to read; the archive is the full audit trail.

| Asked | Question | Page | Status |
| --- | --- | --- | --- |
